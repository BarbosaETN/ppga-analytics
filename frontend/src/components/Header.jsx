function Header({ titulo}) {
    return (
        <header className="header">
            <div className = "header-title">
                <h1>{titulo}</h1>
            </div>
        </header>
  );
}

export default Header;
